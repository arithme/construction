'use client';import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';
export function AdminLogin(){const router=useRouter();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[configured,setConfigured]=useState(true);const[loading,setLoading]=useState(false);useEffect(()=>{fetch('/api/admin/session').then(r=>r.json() as Promise<{authenticated:boolean;configured:boolean}>).then(d=>{if(d.authenticated)router.replace('/admin');setConfigured(d.configured)}).catch(()=>setError('Unable to check the admin session.'))},[router]);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');try{const r=await fetch('/api/admin/session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json() as {error?:string};if(!r.ok)throw new Error(d.error||'Login failed.');router.replace('/admin')}catch(e){setError(e instanceof Error?e.message:'Login failed.')}finally{setLoading(false)}}return <div className="admin-login card"><p className="eyebrow">PRIVATE ADMIN</p><h1>Agency platform</h1><p>Sign in with your database-backed agency account.</p>{!configured&&<div className="error-message">Set <code>DATABASE_URL</code>, run migrations and seed the first administrator.</div>}<form onSubmit={submit}><label className="field">Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="username"/></label><label className="field">Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label>{error&&<p className="error-message" role="alert">{error}</p>}<button className="button" disabled={loading||!configured}>{loading?'Signing in…':'Sign in ↗'}</button></form></div>}
