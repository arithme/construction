'use client';
import {useState} from 'react';import type {MediaItem} from '@/lib/types';import {Dialog,DialogContent,DialogTitle,DialogDescription,DialogTrigger} from './ui/dialog';import {Button} from './ui/button';
export function ProjectGallery({items}:{items:MediaItem[]}){const[index,setIndex]=useState(0);const m=items[index];if(!m)return <p>No project media available.</p>;return <Dialog><DialogTrigger asChild><button className="gallery-trigger"><img src={m.src} alt={m.alt} width="1536" height="1024"/><span>View gallery ↗</span></button></DialogTrigger><DialogContent className="gallery-dialog"><DialogTitle>Project gallery</DialogTitle><DialogDescription>{m.caption||m.alt}{m.isSample?' · Sample concept':''}</DialogDescription>{m.type==='image'?<img src={m.src} alt={m.alt} width="1536" height="1024"/>:m.type==='video'?<video controls src={m.src} aria-label={m.alt}/>:<iframe title={m.alt} src={m.src} loading="lazy" sandbox="allow-scripts allow-same-origin allow-presentation" allowFullScreen/>}<div className="filters"><Button disabled={index===0} onClick={()=>setIndex(i=>i-1)}>Previous</Button><span>{index+1} / {items.length}</span><Button disabled={index===items.length-1} onClick={()=>setIndex(i=>i+1)}>Next</Button></div></DialogContent></Dialog>}

