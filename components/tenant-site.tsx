import type {ClientInput} from '@/lib/client-schema';
import {TenantLeadForm} from '@/components/tenant-lead-form';
export function TenantSite({client,preview=false}:{client:ClientInput;preview?:boolean}){
 const phoneHref='tel:'+client.phone.replace(/[^+\d]/g,'');
 return <div className={'tenant-site theme-'+client.theme} style={{'--accent':client.primaryColor,'--ink':client.secondaryColor} as React.CSSProperties}>{preview&&<div className="demo-strip">DRAFT PREVIEW · NOT PUBLIC</div>}<header className="tenant-header wrap"><a className="wordmark" href="#top">{client.logoUrl&&<img src={client.logoUrl} width="46" height="46" alt=""/>}<span>{client.name}</span></a><nav><a href="#services">Services</a><a href="#projects">Projects</a><a href="#contact">Contact</a></nav><a className="button compact" href="#contact">Get a quote</a></header><main id="top"><section className="tenant-hero"><div className="wrap"><p className="eyebrow">CONSTRUCTION · {client.city.toUpperCase()}</p><h1>{client.tagline||client.name}</h1><p>{client.description||`${client.name} provides construction services in ${client.city} and surrounding areas.`}</p><div className="actions"><a className="button" href="#contact">Discuss your project</a>{client.phone&&<a className="button outline" href={phoneHref}>Call {client.phone}</a>}</div></div></section><section id="services" className="section wrap"><p className="eyebrow">SERVICES</p><h2>What we do</h2><div className="grid three">{client.services.filter(s=>s.enabled).map(s=><article className="card service-card" key={s.slug||s.name}><h3>{s.name}</h3><p>{s.shortDescription||`Planning and coordinated delivery for ${s.name.toLowerCase()} projects.`}</p></article>)}</div></section><section id="projects" className="section muted"><div className="wrap"><p className="eyebrow">PROJECTS</p><h2>Project portfolio</h2><div className="grid three">{client.projects.filter(p=>p.enabled).map(p=><article className="card service-card" key={p.slug||p.name}><h3>{p.name}</h3><p>{p.description||'Project details will be added here.'}</p>{p.location&&<span className="meta">{p.location}</span>}</article>)}</div></div></section><section id="contact" className="cta"><div className="wrap"><p className="eyebrow">START A CONVERSATION</p><h2>Tell us about your project.</h2><TenantLeadForm/><div className="actions">{client.phone&&<a className="button outline" href={phoneHref}>Call now</a>}{client.email&&<a className="text-link" href={'mailto:'+client.email}>{client.email}</a>}</div></div></section></main><footer><div className="wrap footer-bottom">© {new Date().getFullYear()} {client.name}<span>{client.city}, {client.state}</span></div></footer></div>
}
