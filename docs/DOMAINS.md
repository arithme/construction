# Domains

Domain records support generated subdomains and custom hostnames with PENDING, VERIFIED and FAILED states. A domain must never be marked verified without infrastructure confirmation. Set `ROOT_DOMAIN` and platform DNS targets for the selected host before exposing custom-domain instructions.
