import {readFileSync,writeFileSync,mkdirSync,existsSync} from 'node:fs';import {resolve} from 'node:path';const slug=process.argv[2];if(!slug||!/^[a-z][a-z0-9-]{1,50}$/.test(slug))throw new Error('Supply a lowercase client slug: npm run new-client -- client-name');const target=resolve('config/clients',slug);if(existsSync(target))throw new Error('Client already exists; refusing to overwrite.');mkdirSync(target,{recursive:true});const source=readFileSync('config/company.ts','utf8');writeFileSync(resolve(target,'company.ts'),source.replace('Aranya Buildcon',slug.split('-').map(w=>w[0].toUpperCase()+w.slice(1)).join(' ')).replace("shortName:'ARANYA'",'shortName:'+JSON.stringify(slug.toUpperCase())));console.log('Created '+target+'/company.ts. Verify and replace root config/company.ts when ready.');
