import {requireUser,canAccessClient} from '@/lib/admin-session';import {createImageUpload} from '@/lib/storage';
export async function POST(request:Request){const user=await requireUser(request);if(!user)return Response.json({error:'Unauthorized'},{status:401});try{const body=await request.json() as {clientId:string;name:string;type:string;size:number};if(!canAccessClient(user,body.clientId))return Response.json({error:'Forbidden'},{status:403});return Response.json(await createImageUpload(body))}catch(e){return Response.json({error:e instanceof Error?e.message:'Upload rejected.'},{status:422})}}
